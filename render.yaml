services:
  - type: web
    name: sentineliq-backend
    runtime: node
    rootDir: Eagle-vision_Web_Application/backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5000
      - key: MONGO_URI
        fromSecret: MONGO_URI
    autoDeploy: true
